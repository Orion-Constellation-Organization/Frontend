/**
 * Enum que define os itens do menu selecionáveis.
 *
 * @enum {string}
 */
export enum SelectedMenu {
  BEGIN = 'Begin',
  WAITING_VOLUNTEER = 'Waiting_volunteer',
  WAITING_CONFIRMATION = 'Waiting_confirmation',
  CLASSES_SCHEDULED = 'Classes_scheduled',
}
