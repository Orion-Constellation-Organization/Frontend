/**
 * Configurações do ambiente de produção
 * @property {boolean} production - Indica se é ambiente de produção
 * @property {string} urlBase - URL base da API
 */
export const environment = {
  production: true,
  urlBase: 'http://localhost:4444/api', //  TODO:Mudar quando tiver a api
};
